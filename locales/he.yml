auth:
  ? signin
  ? signup
  ? signout
events:
  add: ~
  title: ~
  filter:
    ? style
posts:
  ? add
  ? title
profiles:
  ? add
  ? title
myprofile:
  events:
    ? add
  posts:
    ? add
  title: ~
settings:
  title: ~
  account:
    ? title
    ? description
    ? changepassword
    ? delete
  profile:
    ? title
    ? description
  contacts:
    ? title
    ? description
city:
  ? anywhere
  ? change
objective:
  ? label
role:
  ? label
sort:
  ? popular
  ? newest
profile:
  ? username
  ? community
  ? type
  ? name
  ? bio
  ? hometown
  ? birthday
  ? gender
  ? styles
  ? story
  ? languages
  ? skills
  ? days
  ? topics
  ? teacher
  ? partner
  ? height
  ? weight
  ? partnerBio
  ? objectives
  ? place
  ? visibility
  ? photo
account:
  ? email
  ? password
  ? name
  ? phone
  ? newsletter
agreement: ~
terms: ~
privacy: ~
register:
  ? login
  ? submit
signin:
  ? register
  ? nopassword
  ? submit
nopassword:
  ? signin
  ? google
  ? submit
contacts:
  ? email
  ? website
save: ~
teaser:
  partner:
    ? title
    ? description
    ? btn
  events:
    ? title
    ? description
    ? btn
  profile:
    ? title
    ? description
    ? btn
  allevents:
    ? title
    ? description
    ? btn
  chat:
    ? title
    ? description
    ? btn
  involve:
    ? title
    ? description
    ? btn
share:
  ? title
  ? copy
footer:
  ? about
  ? support
  ? privacy
  ? terms
  ? coc
  ? faq
